VITE_API_BASE=/api
# WebSocket derives from API_BASE → goes through https://crm.landwebpage.co.il/socket.io/
# socket.io falls back to long-polling automatically when WS upgrade is blocked
# (e.g. NetFree returns 418 on wss:// to inspected HTTPS domains).
