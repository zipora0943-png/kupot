VITE_API_BASE=https://crm.landwebpage.co.il/api
# WebSocket bypasses the public domain because NetFree blocks WS upgrades there.
# Goes direct to the backend port over HTTP — fine when the admin is accessed
# via http://178.105.96.70:5000 (no mixed-content block).
VITE_SOCKET_URL=http://178.105.96.70:3000
