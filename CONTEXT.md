# פרויקט קופות — קובץ הקשר

## מה הפרויקט
מערכת ניהול קופות צדקה הפזורות ברחבי הארץ.
קופות מותקנות במיקומים שונים, גובים מרוקנים אותן ומדווחים, וחדר כסף מזין את הסכומים.

## ויירפריים מאושר
הקובץ `kupot_wireframe_v10.html` בתיקייה זו הוא ה-UI הסופי המאושר.
יש לבנות את כל הממשקים בדיוק לפיו — כולל כל הלוגיקה, הפילטרים, והזרימות.

---

## ארכיטקטורה

- **Backend:** Node.js + Express
- **Database:** PostgreSQL
- **Auth:** JWT — 3 תפקידים: `admin` / `collector` / `cashroom`
- **Frontend:** React Native (Android)
- **3 ממשקים נפרדים:** מנהל, גובה (מובייל בלבד), חדר כסף

---

## מודל נתונים

| טבלה | תיאור |
|---|---|
| `boxes` | קופות — מספר ברזל, סוג, סטטוס (uninstalled/active/inactive) |
| `cards` | כרטסות — קופה, מיקום מלא, גובה, שם מותאם, טווח התראה אישי, דרוש קבלה, סטטוס |
| `envelopes` | מעטפות — כרטסת, מספר, תאריך גביה, סכום, סטטוס (pending/entered) |
| `events` | אירועים — כרטסת, סוג, תיאור, משתמש, תאריך, תמונה |
| `tasks` | משימות — קופה, כרטסת, סוג, סטטוס, משויך, הערות, תמונה ביצוע |
| `reports` | דיווחים — כרטסת, סוג, תיאור, סטטוס, גובה, תמונה |
| `users` | משתמשים — שם, תפקיד, שיוכי אזורים (JSON), החרגות (JSON) |
| `task_types` | סוגי משימות — שם, אייקון, opens_card, closes_card |
| `report_types` | סוגי דיווחים — שם, אייקון |
| `box_types` | סוגי קופות |
| `settings` | הגדרות גלובליות (key/value) — כולל alert_days_global |

---

## לוגיקה עסקית קריטית

### כרטסות
- קופה יכולה לעבור מיקום — כרטסת ישנה נסגרת, חדשה נפתחת
- כל פעולה (גביה/דיווח/משימה) משויכת אוטומטית לכרטסת הפעילה של הקופה
- כרטסות סגורות נשמרות לצורך דוחות

### לוגיקת משימות בעת אישור ביצוע
| סוג משימה | פעולה אוטומטית |
|---|---|
| התקנה | פתיחת כרטסת חדשה + אירוע התקנה (חובה להזין מיקום) |
| הסרה | סגירת כרטסת (לא הקופה!) + אירוע הסרה |
| העברת מיקום | סגירת כרטסת קיימת + פתיחת חדשה + 2 אירועים |
| כל שאר | אירוע ביצוע בלבד, ללא שינוי כרטסת |

### שיוך גובה לקופות
- היררכי: עיר → שכונה → רחוב → קופה בודדת
- תמיכה בהחרגות בכל רמה
- גובה רואה רק את הקופות המשויכות אליו

### התראות
- כל כרטסת פעילה שלא בוצע בה אירוע גביה מעל X ימים
- X = טווח אישי לכרטסת אם הוגדר, אחרת גלובלי (ברירת מחדל: 30 יום)

---

## זרימות עבודה עיקריות

**גביה:** גובה בוחר קופה → מזין מספר מעטפה (ברקוד/ידני) → אופציונלי: מדפיס מדבקה → אישור → אירוע גביה נוצר, מעטפה בסטטוס pending

**הזנת סכום:** מזין סורק/מזין מספר מעטפה → מוצגים פרטי הקופה → מזין סכום → אישור → מעטפה עוברת ל-entered

**דיווח → משימה:** גובה מדווח בעיה → מנהל רואה דיווח → המרה למשימה (תוכן מועתק אוטומטית) → בחירה אם לסגור את הדיווח המקורי

---

## מצב פיתוח

| שלב | תיאור | סטטוס |
|---|---|---|
| 1 | Backend + DB + APIs + Tests | ⏳ טרם התחיל |
| 2 | ממשק גובה — React Native | ⏳ טרם התחיל |
| 3 | ממשק חדר כסף — React Native | ⏳ טרם התחיל |
| 4 | ממשק מנהל — React Native | ⏳ טרם התחיל |
| 5 | פיצ'רים משלימים | ⏳ טרם התחיל |

**עדכן טבלה זו בסיום כל שלב.**

---

## הנחיות עבודה

- קרא תמיד את `CONTEXT.md` ואת הקוד הקיים לפני שאתה מתחיל
- הוויירפריים `kupot_wireframe_v10.html` הוא האמת — בנה לפיו בדיוק
- כתוב tests ללוגיקה העסקית הקריטית (כרטסות + שיוך גובה)
- תעד שינויים ארכיטקטוניים חשובים בקובץ זה
